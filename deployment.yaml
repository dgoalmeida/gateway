apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway_k8s
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway_k8s
  template:
    metadata:
      labels:
        app: gateway_k8s
    spec:
      containers:
      - image: gateway:12.15
        imagePullPolicy: IfNotPresent
        name: gateway_k8s
        ports:
        - containerPort: 8080
