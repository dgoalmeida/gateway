node {
    stage("Clone Project") {
        git branch: 'main', url: 'https://github.com/dgoalmeida/gateway.git'
    }

    stage("update image") {
        yq -i '.spec.template.spec.containers[0].image = gateway:$BUILD_NUMBER' < deployment.yaml
    }
}