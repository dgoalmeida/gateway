node {
    stage("Clone Project") {
        git branch: 'main', url: 'https://github.com/dgoalmeida/gateway.git'
    }

    stage("update image") {
        script{ datas = readYaml (file: 'deployment.yml') }
                    echo spec.template.spec.containers[0].image.toString()
        }
    }
}